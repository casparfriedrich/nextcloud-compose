[Unit]
Description=Nextcloud
After=docker.service

[Service]
ExecStart=/opt/nextcloud-compose/nextcloud.sh up
ExecStop=/opt/nextcloud-compose/nextcloud.sh down
Restart=on-failure

[Install]
WantedBy=multi-user.target
